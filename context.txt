========================================================================
             PS BILLING SYSTEM - THERMAL PRINTING & PRODUCTION LOGS
========================================================================

If you start a new chat with an AI assistant, share this file so the assistant 
understands how thermal receipt printing, silent printing, sales visualizations, 
and production logging are configured in this codebase.

------------------------------------------------------------------------
1. PROBLEM DESCRIPTION (PRINT DITHERING / FAINTNESS):
------------------------------------------------------------------------
When printing receipts to a monochrome 1-bit thermal printer (like TVS RP 3160 STAR) 
via Electron's `window.print()`:
- Small fonts (like 10px-11px JetBrains Mono) have thin stems.
- Chromium applies anti-aliasing (gray pixels) at the edges of the font.
- The thermal printer driver converts these gray pixels into a dotted dither 
  pattern to simulate gray, making the letters look faint or invisible.
- Colors like gray (#4b5563) used on slogan/operator details get heavily 
  dithered and print as faint, unreadable dot clusters.

------------------------------------------------------------------------
2. SOLUTION DETAILS (PRINT DITHERING FIX):
------------------------------------------------------------------------
We updated the `@media print` queries in `src/index.css` with the following 
optimizations specifically designed for 80mm/72mm thermal rolls:

- Solid Pure Black: Forced `color: #000000 !important;` on `.print-receipt-area` 
  and all nested elements (`.print-receipt-area *`). This prevents the browser 
  from outputting any gray shades, eliminating dithering on slogans/headers/footers.
- Bold Text: Forced `font-weight: 700 !important;` on all receipt text elements. 
  Bolder text increases the width of the printed stems.
- Stroke Thickness: Added `-webkit-text-stroke: 0.25px #000000 !important;` to 
  thicken text outlines slightly, ensuring the thermal print head burns solid lines.
- System Sans-Serif: Changed `font-family` during printing to standard system 
  sans-serif (`system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial`) 
  which has simpler, cleaner, and thicker stems than JetBrains Mono at small sizes, 
  significantly enhancing Tamil and English legibility.
- Minimum Size: Forced a minimum highly readable print font-size of `12px` 
  via `.print-receipt-area * { font-size: 12px !important; }`, while preserving 
  the main shop title at `18px` via a helper class `.receipt-shop-name`.

------------------------------------------------------------------------
3. DIRECT SILENT PRINTING (NO DIALOG POPUP):
------------------------------------------------------------------------
To match the old software and print directly to the default printer without popping 
up the Electron/Chromium print settings preview window:

- IPC print handler: Added a `print-silent` handler in `electron/main.cjs` that calls 
  `webContents.print({ silent: true, printBackground: true })` on the main window.
- Preload Context Bridge: Exposed `printSilent` to the frontend via `electron/preload.cjs`.
- React Print Trigger: Modified `handlePrint` in `src/components/PrintReceiptModal.jsx` 
  to invoke the silent print IPC call and automatically close the modal 500ms after the 
  print job is queued (giving Chromium enough time to capture the print frame).

------------------------------------------------------------------------
4. SALES TREND VISUALIZATION MODAL:
------------------------------------------------------------------------
Added an interactive sales trend visualizer inside the Sales History page:

- Trigger Button: Added a green "வரைபடம் / Visualize Sales" button inside 
  `src/components/SalesHistory.jsx` next to the "Export CSV" button.
- Aggregations: Dynamic filters summarizing transaction history by:
  - Daily Trend (last 7 days)
  - Weekly Trend (last 4 weeks)
  - Monthly Trend (last 6 months)
  - Yearly Trend (last 3 years)
- SVG Interactive Charts: Custom render system using responsive vector shapes (<rect>), 
  gradients, gridlines, hover listener nodes, and absolute HTML tooltips displaying 
  exact value popups (e.g. ₹338.00) on cursor hover.
- Summary Stats: Displays Total Sales, Average Sales, Peak (Max) Sales, and growth comparison.
- Keyboard Close Handler: Pressing `Escape` while the chart visualizer is open will 
  close the visualizer modal. Pressing `Escape` again will navigate back to the 
  billing dashboard.

------------------------------------------------------------------------
5. PRODUCTION LOGGING SYSTEM (app.log WITH AUTO-ROTATION):
------------------------------------------------------------------------
To aid in remote debugging without cluttering the UI, we implemented a full 
production logging system:

- Log Storage Path: Saved to `app.log` in the application's root user data folder 
  (the same folder containing `database.json`, accessible in the OS filesystem at 
  `AppData\Roaming\PS Billing System\`). It cannot be accessed from the application UI.
- Lightweight Auto-rotation: Includes a strict limit check. If `app.log` exceeds **1 MB**, 
  it is renamed to `app.log.old` (replacing the previous `.old` file). This caps the 
  total log storage strictly at **2 MB** max, ensuring the app remains light and manageable.
- Recorded Events:
  - System Lifecycle: App start, startup platform metadata, user data folder paths, window loads.
  - IPC Operations: Window login/logout calls, database reads/writes (along with performance/speed 
    durations and data summary metrics), and silent print triggers.
  - Renderer User Actions: Exposes a `logMessage` bridge API. React components log logins, logout, 
    bill saves, bill deletions, visualizer open/close events, and CSV export triggers.
  - Crashes & Errors: Intercepts `uncaughtException` in the main process, `did-fail-load` window 
    load failures, and warnings/errors originating in the React console (`console-message` handler).

------------------------------------------------------------------------
6. DUPLICATE ITEM CHECK DIALOG (PREVENT DUPLICATIONS):
------------------------------------------------------------------------
To prevent accidental duplications when a cashier scans or enters the same item 
code multiple times:

- Duplicate Detection: When adding a product (either by typing code directly and 
  pressing Enter, or selecting from the search overlay, or clicking search sidebar), 
  the app verifies if that item code is already added in another row.
- Choice Dialog Modal: If a duplicate is found, a dialog appears presenting 2 choices:
  1. "நிலவும் பொருளின் அளவை மாற்றவும் / Edit existing item quantity (Recommended)"
     Focuses the cursor onto the `qty` column of the existing product row so the 
     cashier can immediately adjust the weight/quantity. Clears the temporary duplicate row.
  2. "புதிய வரியாக சேர்க்கவும் / Add as a new row (new weight/qty)"
     Proceeds to add a new line with the duplicate code (useful for multi-weight slabs).
- Keyboard Navigation: The cashier can use the Arrow keys (Up/Down or Left/Right) to 
  change selection, press Enter to confirm the selected action, or press Escape 
  to dismiss/cancel.
- Logged Event: The selection choice is written to the production log for audit tracking.

------------------------------------------------------------------------
7. CUSTOM SETTINGS & MANAGEMENT PANEL (FUTURE-PROOFING FOR OTHER SHOPS):
------------------------------------------------------------------------
To allow reusing the POS system for other shops, we implemented a complete settings 
and database management console (allowing customization of Shop Name, slogan, phone 
numbers, operator and options to wipe sales/products).

- Father's App Safety: To eliminate any risk of accidental database wipes or settings 
  misconfigurations by cashiers in your father's active system, the Settings panel 
  has been removed from the primary workspace.
- PS_BILLING Template Clone: The full settings-enabled codebase has been cloned 
  to the `PS_BILLING/` directory in the main projects folder (alongside the `ps/` repository).
- Future Shop Setup: If you want to configure and package the app for another shop, 
  simply use and modify the project inside the `PS_BILLING/` folder.

------------------------------------------------------------------------
8. MODIFIED FILES:
------------------------------------------------------------------------
1. `src/components/PrintReceiptModal.jsx`
   - Added class `className="receipt-shop-name"` to the shop name <div>.
   - Updated `handlePrint` to call `window.electronAPI.printSilent()`, log actions, and auto-close.
2. `src/index.css`
   - Completely replaced the `@media print` block with optimized thermal printing styles.
3. `electron/preload.cjs`
   - Exposed `printSilent` and `logMessage` to `window.electronAPI`.
4. `electron/main.cjs`
   - Implemented `writeLog` logging system with 1MB file rotation.
   - Added IPC handlers for `print-silent` and `log-message`.
   - Logged database reads/writes, window changes, lifecycles, and exceptions.
5. `src/components/SalesHistory.jsx`
   - Added imports, visualization states, helper data calculation functions.
   - Inserted "Visualize Sales" trigger button in the header.
   - Updated `Escape` key event listener.
   - Embedded `SalesVisualizerModal` rendering code.
   - Logged CSV exports, deletions, and visualizer tab changes.
6. `src/components/BillingDashboard.jsx`
   - Added duplicate prevention dialog with keyboard support.
   - Implemented focus management and navigation bypasses.
   - Logged component mounting, saves of bill edits, and saves of new bills.
   - Note: Customizable Settings panel additions were cloned to the parent folder `PS_BILLING/` and reverted here for active safety.

------------------------------------------------------------------------
9. REFERENCE CSS CODE BLOCK (from src/index.css):
------------------------------------------------------------------------
/* Print CSS Styles */
@media print {
  @page {
    margin: 0 !important;
  }
  html, body {
    margin: 0 !important;
    padding: 0 !important;
    background: #ffffff !important;
  }
  body * {
    visibility: hidden;
  }
  .print-receipt-area, .print-receipt-area * {
    visibility: visible !important;
    color: #000000 !important;
    background: transparent !important;
    font-weight: 700 !important; /* Force bold/thick text for clear thermal print */
    -webkit-text-stroke: 0.25px #000000 !important; /* Thicken text outlines slightly to prevent faint print */
    text-shadow: none !important;
    font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
  }
  .print-receipt-area * {
    font-size: 12px !important; /* Force minimum highly readable font size for all print contents */
  }
  .print-receipt-area .receipt-shop-name {
    font-size: 18px !important; /* Keep the shop name large and prominent */
  }
  .print-receipt-area {
    position: absolute;
    left: 0;
    top: 0;
    width: 72mm !important; /* Fits perfectly on standard 80mm thermal rolls */
    padding: 4mm 4mm 12mm 4mm !important; /* Safe margins and bottom tear-off space */
    font-size: 12px !important;
    line-height: 1.3 !important;
    background: #ffffff !important;
    box-sizing: border-box !important;
  }
  .print-receipt-area th, .print-receipt-area td {
    border: none !important;
    padding: 2px 0 !important;
    color: #000000 !important;
  }
}
------------------------------------------------------------------------

------------------------------------------------------------------------
10. TAURI RUST MIGRATION SUBPROJECT (tari/):
------------------------------------------------------------------------
To optimize packaging size (from ~85MB to ~5MB) and RAM footprint (from ~220MB 
to ~35MB), we initiated a Tauri subproject inside the `tari/` folder:

- Folder Location: `tari/` (configured to be ignored in the root git repo to prevent commits bloat).
- Tech Stack: Vite + React on frontend, Rust on backend (`src-tauri/`).
- Rust Commands (`src-tauri/src/lib.rs`):
  - `db_read` / `db_write`: Fast standard library file I/O for `database.json`.
  - `log_message`: Appends frontend logs to `app.log` with 1MB rotation.
  - `print_silent`: Spools standard printing via native system handlers.
- JS Adapter (`tauriBridge.js`): Maps window control, logging, and database operations.
- Dynamic Resolution: Resolves window maximize triggers automatically on boot and success states, accommodating his father's target 1360*768 screen resolution.
- Compilation: Built successfully using the MSVC toolchain and Windows SDK. Standalone executable: `tari/src-tauri/target/release/app.exe`. NSIS Installer: `tari/src-tauri/target/release/bundle/nsis/ps-billing-system_0.1.0_x64-setup.exe`. MSI Installer: `tari/src-tauri/target/release/bundle/msi/ps-billing-system_0.1.0_x64_en-US.msi`.
- Cleanup: Removed root `electron/` directory and stripped Electron builders, package configs, dependencies, and build configurations from root `package.json`.
- Icon Setup: Generated a square padded version of the shop logo (`logo_square.png`) and updated the Tauri bundle icons. Configured `tauri.conf.json` with explicit `bundle.windows.nsis.installerIcon` settings to brand both the standalone `.exe` and the setup installers. Note: If Windows Explorer shows the default logo, copy/rename the file to refresh Windows' local icon cache.
- Startup State: Set `"maximized": true` and `"resizable": true` in `tauri.conf.json` so the app launches full-screen right from the login boot screen. Disabled window shrinking inside `tauriBridge.js`'s `onLogout` implementation to keep the login screen full-screen at all times.
- Silent Printing: Configured `WEBVIEW2_ADDITIONAL_BROWSER_ARGUMENTS` environment variable with `"--kiosk-printing"` inside `lib.rs`'s `run()` hook on Windows, forcing WebView2 to print silently to the default printer without displaying print options or dialogs. Rewrote `PrintReceiptModal.jsx` to render the printable receipt layout strictly off-screen and trigger the print immediately on mount, completely bypassing the bill preview window/screen overlay. Updated F11 & F12 keydown listeners in `BillingDashboard.jsx`'s read-only viewing mode to directly trigger Tamil/English reprint, avoiding stock and database edits.
- History Detail Modal: Added a new "பார்க்க / View" button in `SalesHistory.jsx` that opens a modal displaying the complete bill details (customer metadata, itemized tables, pricing totals) and options to print directly in Tamil or English.
- Dynamic Date Sync: Updated the date display inside the Billing Dashboard metadata card to dynamically show the actual date of the saved bill being inspected when viewing past invoices.
- Keybinding Refactor: Removed redundant `F2` keybinding from `BillingDashboard.jsx` and `features_guide.md`, standardizing on the instant `Enter` key trigger on empty code cells for mouse-free fuzzy item searches.
- Slab Pricing Investigation & August Database Discovery:
  - Discovered that the shop computer was upgraded to `Sms3` and actively running right up to August 18, 2026.
  - Copied and verified `Old_sms/Sms3/Data` containing 1,803 products with up-to-date August prices and 1,850 bills up to Bill #523 on August 18, 2026 at 12:06 PM.

------------------------------------------------------------------------
11. STORE USABILITY, RECEIPT LAYOUT & NAVIGATION REFINEMENTS (AUGUST 2026):
------------------------------------------------------------------------
Following store usage feedback from the operator/father, the following 3 key enhancements were implemented across the app:

1. Thermal Receipt Layout Refinement (`tari/src/components/PrintReceiptModal.jsx`):
   - Prominent Top Phone Number: Relocated phone number (`📞 9629708861`) right below the store name at the top in large, bold text.
   - Clean Single Total Display: Replaced separate Gross and Net lines with a single, clear `மொத்தம் / Total : ₹...` line.
   - Handwritten Notes Buffer: Added an empty buffer space with guide lines between the items list and totals so extra items can be added by hand if necessary.

2. Billing Search Overlay & High-Contrast Navigation (`tari/src/components/BillingDashboard.jsx` & `tari/src/index.css`):
   - Green Header: Styled search table header in vibrant Green (`#15803d` / `#166534`) with crisp white typography.
   - Reordered Columns: `குறியீடு / Code`, `தமிழ் பெயர் / Tamil Name` (1st name col), `பொருள் / Product Name (Eng)`, `Unit` (centered), `விலை / Rate` (compact). Removed redundant `Price Type` column.
   - Solid Dark Blue Active Highlighter: Selected row is styled with a Solid Dark Blue (`#1d4ed8`) background and pure white (`#ffffff`) text for immediate, crystal-clear differentiation.
   - Stable List Navigation: The search table remains visually stable while the selection cursor moves row-by-row on ArrowUp/ArrowDown, scrolling smoothly via `scrollIntoView({ block: 'nearest' })`.

3. Product Management Master Table & Slab Keyboard Traversal (`tari/src/components/ProductManager.jsx` & `tari/src/index.css`):
   - Matched Table Layout: Styled the main product list with the exact same Green header (`#15803d` / `#166534`), column order (`Code`, `Tamil Name`, `English Name`, `Unit`, `Rate`, `Actions`), compact widths, and Solid Dark Blue highlighter (`#1d4ed8`) as the Billing search table.
   - Stable Product List Scrolling: Integrated `scrollIntoView({ block: 'nearest' })` on Arrow keys, eliminating jarring viewport jumping.
   - Removed Stock Fields: Removed `முன் இருப்பு / Opening Stock` and `தற்போதைய இருப்பு / Current Stock` completely from the edit form.
   - Unlocked Numeric Arrow Traversal: Removed text-selection constraints on numeric inputs (`sellingPrice`, `netPrice`, `mrp`, `costPrice`, `slabQty_i`, `slabOffset_i`), enabling instantaneous 4-way arrow key movement.
   - 100% Keyboard & Arrow-Key Navigation into Slabs:
     - `ArrowDown` from `Cost Price` / `MRP` moves into `+ சேர்க்க / Add` and each `Slab Grams` & `Offset` input, and then down to `Save`/`Cancel`.
     - `ArrowRight` from `Grams` jumps to `Offset`; `ArrowRight` from `Offset` jumps to `Delete`; `ArrowLeft` returns.
     - `Enter` traverses each field sequentially and deletes a row when pressed on the Delete button.
   - `+ சேர்க்க / Add` Button Styling: Styled with clean White background by default, turning into **Solid Blue (`#2563eb`)** with a bright focus ring when active/focused.
   - `🗑️ நீக்கு / Delete` Button Format: Replaced the small icon with a solid labeled button (`.btn-slab-delete`). Appears in soft red (`#fef2f2`) by default, turning into **Solid Red (`#dc2626`) with bright white text and focus ring (`#1d4ed8`)** when navigated to via keyboard.
   - `சேமி / Save` & `ரத்து செய் / Cancel` Focus Styling: Both action buttons feature high-contrast blue focus states (`#2563eb`) so the active button is unmistakably clear.

------------------------------------------------------------------------
12. AUGUST 2026 DATABASE & SLAB PRICING IMPORT (AUGUST 18, 2026):
------------------------------------------------------------------------
Successfully imported the live store database from `Old_sms/Sms3/Data/` into the POS application's active `database.json`:

1. Backup Protocol:
   - Full backup created before changes: `database_backup_2026_08_18.json` in both `%APPDATA%\com.perumalstores.psbilling\` and the project root.

2. Products & Live Pricing (`IM012026.DBF` & `UN012026.DBF`):
   - Total Products Ingested: **1,803 products** with up-to-date August selling rates, MRPs, cost prices, and category groups.
   - Unit Master Resolution: Decoded 33 unit codes (`_0L10SVN9Y` -> `KG`, `_1RC0RV002` -> `NO`, `_S7P0VC78V` -> `PKT`, etc.) to map proper POS units (`kg`, `piece`, `packet`, `litre`).
   - 100% Tamil Names Preserved: All custom Tamil product names previously entered in the app were merged and retained intact.
   - Slab Pricing Ingestion:
     - 4 Products with custom FoxPro DBF slab offsets (`SOUNF`, `OMUM`, `T.V.S.GROW.OIL`, etc.) imported with their exact weight limits and offset values (`SQ1..SQ4`, `SRATE1..SRATE4`).
     - 155 Weight-based (`kg`) products automatically initialized with the standard 4 weight tiers (50g, 100g, 250g, 500g with `offset: 0`).

3. Historical Invoices & Itemized Lines (`ET012026.DBF` & `ED012026.DBF`):
   - Total Sales Invoices Ingested: **1,850 bills** spanning from July 05, 2026 up to **August 18, 2026 12:06 PM (Bill #523)**.
   - Itemized Rows Ingested: **14,011 individual line items** mapped with quantities, rates, amounts, and Tamil product references.
   - Sales History & Trend Charts: Fully populated with accurate real-world revenue and product velocity data.

------------------------------------------------------------------------
13. DATA DURABILITY AUDIT & REPAIR (AUGUST 20, 2026):
------------------------------------------------------------------------
An audit of the Tauri app in `tari/` against the live shop database found the
billing flow sound but almost nothing protecting the money already recorded.
Six defects could destroy or corrupt saved bills through ordinary keystrokes.
All were fixed, verified against the real 1,850-bill database, and exercised in
the running app. Branch: `durability-fixes` (4 commits).

1. DEFECTS FOUND AND FIXED:

   a) Deleting one bill deleted up to 120 (`SalesHistory.jsx`)
      - Delete matched on `invoiceNo`, which is NOT unique. The 1,850 imported
        bills share only 138 distinct numbers; 1,817 of them (98.2%, Rs11,24,864)
        sat on a duplicated number. Deleting #504 removed 120 bills at once and
        restored stock for only one of them.
      - FIX: every bill now carries an immutable `id`. `handleDeleteTransaction`
        takes the transaction object and matches on `id`. Verified: a sweep over
        all 1,850 bills removes exactly one, every time.

   b) Invoice numbers collided by design (`BillingDashboard.jsx`)
      - `getNextBillNumber()` returned `todayTx.length + 1`. Deleting a bill
        handed its live number to the next customer.
      - FIX: numbers come from `settings.billCounter = { date, next }`, which is
        written in the SAME `onUpdateDatabase` call as the bill. Daily reset is
        preserved (#1 each morning) but a number is never reissued. If the
        counter is missing or stale it falls back to max(today invoiceNo) + 1,
        so a number already on a printed bill is never handed out twice.

   c) Every historical bill reprinted as "Rs NaN" (`PrintReceiptModal.jsx`)
      - The August import wrote line items as { rate, amount, total }; the app
        reads { overridePrice, totalPrice, priceType }. All 1,850 bills were
        affected - every line printed RsNaN, only the footer total was right.
        Legacy items also had no `priceType`, so weighed goods hit the
        `parseInt` branch and a 0.5 kg line printed as quantity 0.
      - FIX: new `src/utils/normalize.js` converts either shape to one canonical
        shape, applied inside `readDatabase()`. PrintReceiptModal coerces every
        figure through a `money()` helper so a malformed item prints 0.00.

   d) Viewing an old bill and pressing F10 zeroed it (`BillingDashboard.jsx`)
      - F11/F12 guarded against read-only view mode; F10 did not. PageUp then
        F10 rewrote the totals from the missing `totalPrice`, turning Rs276.00
        into Rs0.00 with no confirmation.
      - FIX: F10 now carries the same guard. Also added `e.repeat` rejection on
        F10/F11/F12 and an `isSavingRef` in-flight lock, so a held or
        double-tapped key cannot commit two bills.

   e) No backups, and a power cut could truncate the only copy (`lib.rs`)
      - `db_write` used temp+rename (correct shape) but never flushed before the
        rename, used a fixed `database.json.tmp` shared by concurrent commands,
        and no copy of the database existed anywhere.
      - FIX: `write_file_atomic()` calls `sync_all()` before the rename and uses
        a temp name carrying pid + nanosecond stamp. A `DbLock(Mutex)` in Tauri
        state serialises writes. `refresh_backups()` keeps
        `backups/database-YYYY-MM-DD.json` for 7 days plus
        `database-monthly-YYYY-MM.json` kept indefinitely. `db_read` no longer
        seeds over a corrupt live file - it recovers from the newest parseable
        backup, renames the bad file to `database.corrupt.<stamp>.json`, and
        errors rather than seeding if no backup is usable.

   f) The shipping app was not in version control
      - `.gitignore` ended with `tari/`; `git ls-files tari` returned nothing.
        Only the stale Electron copy in `src/` was tracked, diverged by 1,296
        lines. (This is also why `oxlint` reported "No files found to lint" when
        run inside `tari/`.)
      - FIX: `tari/` is committed. `.gitignore` now excludes only build output
        (`tari/src-tauri/target/`, 1.4 GB) and live shop data, and explicitly
        re-includes the app artwork (`!tari/public/*.png`,
        `!tari/src-tauri/icons/**`) which the blanket `*.png` / `*.ico` rules
        had been silently excluding - without those a fresh clone cannot build.
        The superseded `src/`, `dist-desktop/` and `dist-installer/` copies were
        deleted in a separate commit.

2. WHOLE-RUPEE ROUND-OFF (BEHAVIOUR RESTORED):

   Discovered while verifying the edit path: netTotal = round(gross - discount
   + rent + coolie) reproduces ALL 1,850 legacy bills exactly, and the old
   FoxPro export carries a non-zero `roundOff` field on exactly 752 of them.
   The shop has always billed in whole rupees; the new app was producing paise
   totals, and re-saving an old bill drifted it by up to 50 paise.

   - `calculateRawNetTotal()` holds the unrounded figure; `calculateNetTotal()`
     returns `Math.round()` of it; `calculateRoundOff()` is stored on the bill.
   - The receipt prints a Round Off line when it is non-zero.
   - Money is rounded to paise via `round2()` on save, so values like
     130.00000000000003 can no longer reach the database.

3. UNIT / PRICE-TYPE REPAIR:

   726 of 1,803 products in `IM012026.DBF` point at a unit row that no longer
   exists in `UN012026.DBF` (623 dangling codes + 103 blank). Those unit rows
   were deleted from the FoxPro unit master years ago while products kept
   referencing them - this is a hole in the source data, NOT an import bug. The
   11 missing codes appear in none of the three legacy installs (SMS, SMS1,
   Sms3), and the product master weight flags (`IWEIGHT`, `WF`, `WGTAA`, `RBW`,
   `MINWEIGHT`) are uniformly unset on every product, so nothing can recover
   them.

   `import_old_data.py` fell back to `units_map.get(code, "piece")` and
   `priceType = "Quantity" if unit in (kg, litre) else "Fixed"`, so 620 became
   Fixed - meaning `Math.round()` on quantity, and no slab pricing. Real
   consequence: 2.5 kg of IDLY RICE became 3 kg at the counter.

   - REPAIRED: 29 products the sales history PROVES were sold in fractions were
     set to `priceType: 'Quantity'`. Where the unit was also an unresolved code,
     it became `kg` (17 products); where it already read as a real unit, the
     unit was left alone (12 products).
   - NOT GUESSED: the "unit is kg/litre implies Quantity" rule was rejected -
     64 products are labelled `litre` including rice and flour sold by the bag,
     so the unit column itself cannot be trusted as evidence.
   - The remaining unresolved units are now VISIBLE rather than silently wrong:
     new `src/utils/units.js` renders them as "not set" in ProductManager and
     "?" in the billing grid. The unit dropdown carries a synthetic option so
     the current value is never invisible, and picking kg/litre now sets
     `priceType: 'Quantity'` automatically.

4. NEW FILES:

   - `tari/src/utils/normalize.js`  - canonical transaction/line-item shape,
     `round2()`, `indexProducts()`, `normalizeDatabase()`. `unit` and
     `priceType` are read from the product master first, so repairing a product
     also repairs how its old bills read.
   - `tari/src/utils/units.js`      - `UNIT_OPTIONS`, `isUnresolvedUnit()`,
     `unitLabel()`, `billingUnitLabel()`.
   - `tari/scripts/migrate_database.mjs` - one-time database repair (below).
   - `tari/scripts/archive_history.mjs`  - reversible history archive (below).

5. SCRIPTS (all idempotent, all back up before writing):

   # repair the database - ids, line-item shape, drop dead `bills`, seed the
   # counter, fix price types. --dry-run reports without writing.
   cd tari && node scripts/migrate_database.mjs --dry-run
   cd tari && node scripts/migrate_database.mjs

   # move imported history out of the live database into an archive file
   cd tari && node scripts/archive_history.mjs --dry-run
   cd tari && node scripts/archive_history.mjs
   cd tari && node scripts/archive_history.mjs --restore
   cd tari && node scripts/archive_history.mjs --before=01/09/2026

   # correct the sign of slab offsets imported from FoxPro (see item 9)
   node tari/scripts/fix_slab_offsets.mjs --dry-run
   node tari/scripts/fix_slab_offsets.mjs

   All three accept --db=<path> so they can target another machine copy of the
   database rather than the live one.

   IMPORTANT ORDERING inside migrate_database.mjs: products are repaired FIRST,
   then transactions are normalised against the REPAIRED master. Doing it the
   other way bakes the old, wrong `priceType` into all 14,011 line items.

   The scripts abort before writing if integrity checks fail - transaction
   count changed, a bill has no id, duplicate ids, a non-numeric line total, or
   the recorded rupee total changed by even a paisa.

6. APPLIED TO THE LIVE DATABASE (%APPDATA%\com.perumalstores.psbilling\):

   - Migration: 1,850 ids added, 14,011 line items converted, the dead `bills`
     array dropped (a byte-identical duplicate of `transactions` that nothing in
     the app reads), counter seeded, 29 products retyped.
     Size 9.73 MB -> 4.37 MB. Recorded sales unchanged at Rs11,58,415.00.
   - History archive: all 1,850 bills (05/07/2026 - 18/08/2026) moved to
     `history_archive_2026-08-20.json`. The old FoxPro software remains the
     record for that period; holding the same bills in two places invited
     someone to correct one and not the other.
     Size 4.37 MB -> 548 KB. Products, prices, slabs and the counter untouched.
     A restore was verified to return exactly 1,850 bills and the same total.

   NOTE: the 29 unit/price-type repairs were DERIVED from the sales history but
   are written onto the products, so they survive the archive.

7. VERIFICATION PERFORMED:

   - `cargo test --lib` - 3 tests pass: atomic write replaces the file and
     leaves no temp behind; pruning keeps the newest 7 dailies and never touches
     the monthlies; pruning is a no-op under the limit.
   - Logic replay over all 1,850 real bills: 0 NaN lines, 0 zero-quantity lines,
     0 bills zeroed on re-save, 0 totals drifting, every delete removes exactly
     one, no duplicate ids, rupee total preserved.
   - Driven in the running app (vite dev + browser): PageUp shows real amounts;
     three F10 presses on a saved bill leave storage byte-identical; 2.5 kg of
     IDLY RICE stores as 2.500 and prices at Rs125.00; gross Rs134.50 bills as
     Rs135.00 with `roundOff` 0.50; two F10 presses create exactly one bill and
     advance the counter once.
   - Re-run with an empty ledger: billing screen, PageUp, Sales History and all
     four trend charts (Daily/Weekly/Monthly/Yearly) render with no NaN,
     undefined or Infinity, and no console errors.

8. STILL OUTSTANDING:

   RESOLVED SINCE THIS SECTION WAS FIRST WRITTEN:
   - The branch was merged and pushed; everything is on `main`.
   - `backups/` was created on the first real save (20 Aug 16:56), holding both
     database-2026-08-20.json and database-monthly-2026-08.json. The backup
     system is confirmed working end to end.
   - The installer has been rebuilt and carries every fix. See item 12 below
     for how to deploy it.

   STILL OPEN:
   - PRINTING HAS NEVER BEEN TESTED ON THE THERMAL PRINTER. Everything else was
     verified by replaying real data or by driving the app, but no receipt has
     been printed since the layout changed. Print one and check: phone number
     clearly larger than the item rows, one total only, three ruled lines below
     it, jeera 50g at Rs24.50.
   - 464 of the 1,803 products have no selling price and none are flagged
     disabled, so they add a Rs0.00 line silently. Left exactly as SMS holds
     them, at the shopkeeper request. The usable catalogue is 1,339 products.
   - Not addressed: login is still hardcoded T/T and every bill records
     `operator: 'PS'`, so the ledger cannot attribute a sale to a person. Fine
     for a single till; revisit if a second person works the counter.
   - Not addressed: at ~44 bills/day the file will reach roughly 40 MB per year
     of its own history. Splitting history per financial year is the next step,
     and `archive_history.mjs --before=` already supports doing it by hand.

9. SLAB OFFSET SIGN CORRECTION (found after the audit, same day):

   SYMPTOM: JEERA (M47) at 50g billed Rs13.50. The old software bills Rs24.50.

   CAUSE: the old system stores its slab differential in `IM012026.DBF` as
   WQ1..WQ4 (quantities) and WRATE1..WRATE4 (differentials), and applies it as

       effectiveRate = SRATE - WRATE

   M47 has SRATE 380 and WRATE1 -110, so the shop charges 380 - (-110) = 490/kg
   and 50g costs Rs24.50. The Aug 18 slab ingestion copied WRATE straight into
   `offset`, but the app applies

       effectiveRate = sellingPrice + offset

   giving 380 + (-110) = 270/kg and Rs13.50 - undercharging by Rs11 on every
   50g sale of an expensive spice. Some products were worse: AACHIKULAMBU50GM
   (116) computed a NEGATIVE rate of -Rs2/kg.

   NOTE: the slab values are in the W-series (WQ/WRATE), not the S-series
   (SQ/SRATE) that section 12 above refers to. On M47 the S-series is all
   zeros. Anything re-importing slabs must read WQ1..WQ4 / WRATE1..WRATE4 and
   NEGATE the rate, because the app adds the offset where FoxPro subtracts it.

   `tari/import_old_data.py` in this repo is NOT the culprit - it always writes
   `"slabs": []`. The script that ingested the slabs on Aug 18 is not in the
   repo. If it resurfaces, this is the line to fix.

   EVIDENCE: checked against the 1,850 real bills, which record what customers
   were actually charged. Of the 287 slab bands with sales history:

     - 57 were wrong before and are correct after the negation
     - 0 were broken by the negation
     - 120 were already correct (offset 0, unaffected)
     - 110 match neither, because the base selling price has changed since
       those July sales - not a slab problem

   M47 after the fix, against the old software: 50g Rs24.50, 100g Rs49.00,
   250g Rs95.00, 500g Rs190.00, 1kg Rs380.00 - every band matches.

   FIX APPLIED: `tari/scripts/fix_slab_offsets.mjs` negates the offsets of the
   75 affected products. It only touches slabs that still match the FoxPro
   source exactly, so any slab edited by hand in the app since the import is
   left alone (0 such products at the time it ran). It backs up first, and
   aborts if the correction does not improve agreement with real sales.

       node tari/scripts/fix_slab_offsets.mjs --dry-run
       node tari/scripts/fix_slab_offsets.mjs

   The app code was NOT changed. `sellingPrice + offset` stays the convention,
   because that is what the Product Manager screen shows and what a shopkeeper
   typing a new slab by hand would expect. Only the imported data was wrong.

   CONSEQUENCE FOR PRICES: 75 products now charge MORE at small quantities than
   the app was charging before this fix - which is what the shop has always
   charged. Worth spot-checking a few against the old software before a busy
   day.

10. BUNDLED SEED REBUILT FOR FRESH INSTALLS:

    `tari/src-tauri/seed_database.json` is compiled into the binary with
    `include_str!` and written to AppData by `db_read` ONLY when no database
    exists yet (`if !db_path.exists()`). An install over an existing database
    never touches it.

    The old seed carried slabs on 1 of its 1,803 products, and that single entry
    used a value the shop does not charge. A clean install would therefore have
    billed every weighed item at its flat base rate - jeera 50g at Rs19.00
    instead of Rs24.50.

    Regenerated from the live database after the offset sign correction:

      products                    1,803  (all with English + Tamil names)
      with a selling price        1,339
      with slab pricing              75  (was 1)
      weighed, Quantity type        198
      product groups                 11
      transactions                    0  (empty by design)
      settings                    shop name, slogan, phone numbers
                                  billCounter dropped, so a fresh install
                                  starts its own numbering

    Verified the rebuilt binary embeds `"offset": 110` and no longer contains
    `"offset": -110`.

    NOTE: the seed is a snapshot. Prices edited in the app afterwards do not
    flow back into it; it only matters for a fresh install on a clean machine.

11. RECEIPT LAYOUT (to the shopkeeper's specification):

    Three requirements were given: phone number at the top and large, exactly
    one total, and blank space so items can be added by hand after printing.

    a) Phone number was printing at body-text size
       `src/index.css` forces `.print-receipt-area * { font-size: 12px
       !important }` for thermal legibility, and only `.receipt-shop-name` was
       exempted at 18px. The inline 13px on the phone line therefore never
       applied - it printed at 12px, the same size as the item rows.

       The phone line now carries a `receipt-phone` class with its own print
       rule at 17px, centred, letter-spaced, and with a heavier
       `-webkit-text-stroke` so it survives thermal fading. Two classes beat the
       single-class blanket rule on specificity, exactly as the shop name
       already does.

       This matters because the customer keeps the slip and thermal print fades
       over months - the number has to stay readable.

    b) Hand-writing space moved BELOW the total
       The 44px gap sat between the last item and the total, so an item written
       there left the printed total underneath it wrong and needing to be struck
       out. It is now three ruled lines after the total, giving room for the
       extra items and a corrected figure beneath them.

    c) One total only
       Already true before these changes - the receipt shows a single
       `மொத்தம் / Total :` line. The Gross and Net pair seen on receipts printed
       before 20 Aug came from the older build. `labelGross` and `labelNet` were
       left over unused and have been removed.

    The 📞 symbol was kept, at the shopkeeper's request, matching the receipts
    the shop already issues.

    Printed order, captured from the rendered component:

       ஸ்ரீ முருகன் துணை
       SRI PERUMAL STORES
       📞 9629708861                     <- 17px, centred
       BILL NO / DATE / CUSTOMER / TIME
       item rows
       [Discount / Charges / Round Off, each only when non-zero]
       மொத்தம் / Total :
       [three ruled lines for hand-written items]
       ITEMS / thank-you / operator

12. HOW TO DEPLOY TO THE SHOP MACHINE:

    The installer carries the app and the seed. It does NOT carry the live
    database, and it will not repair one that already exists.

    - Fresh machine, no database: install, launch, done. The seed provides all
      1,803 products with prices and slab pricing.
    - Machine that already ran an earlier build: uninstall FIRST and tick
      "Delete the application data" on the uninstaller's confirm page. That
      checkbox is what runs `RmDir /r "$APPDATA\com.perumalstores.psbilling"`.
      Leave it unticked and the old, unrepaired database survives the reinstall.
      Note the generated NSIS script skips the deletion entirely when
      `$UpdateMode = 1`, so installing over an existing copy never clears data.
    - To carry a repaired database to another machine instead, copy
      `%APPDATA%\com.perumalstores.psbilling\database.json` across after
      installing and launching once.

    Installer (both paths are the same file):

      ps-billing-system_latest_setup.exe
      tari/src-tauri/target/release/bundle/nsis/ps-billing-system_0.1.0_x64-setup.exe

------------------------------------------------------------------------
14. PRINTING FIXES & COUNTER USABILITY (AUGUST 20-24, 2026):
------------------------------------------------------------------------
Found by the shopkeeper in real use after section 13 shipped. Two printing
faults and four things that slowed him down at the counter. Prices, slabs,
round-off, bill numbering and the database layer are untouched by all of it.

*** ITEMS 3 AND 4 BELOW WERE REVISED ON 25 AUG - SEE SECTION 15. Filtering was
    right for name searches but wrong for code searches, which now jump within
    the full list instead. ***

1. PRINTING PRODUCED A BLANK SHEET:

   The receipt is rendered inside a host div held off-canvas so it never shows
   on screen:

       position: fixed; left: -9999px; top: -9999px;
       width: 1px; height: 1px; overflow: hidden

   The print stylesheet gives `.print-receipt-area` `position: absolute` with
   `left: 0; top: 0`. Absolute positioning resolves against the nearest
   positioned ancestor - that host - so "0" meant -9999px, and the host's 1x1
   overflow box clipped whatever was left. The page came out blank.

   FIX: the host now carries a `print-receipt-host` class that the print
   stylesheet resets to `position: static; left/top/width/height: auto;
   overflow: visible`, so the receipt lays out on the page instead.

   Measured by applying the @media print rules as screen rules:

       without the fix   left -9999, top -9999, clipped by the host
       with the fix      left 0, top 0, 272 x 448 px, not clipped

   NOTE FOR ANYONE EDITING THE RECEIPT: this off-canvas host is load-bearing on
   screen and hostile to printing. Any new print rule has to survive it.

2. THE WINDOWS PRINT DIALOG APPEARED ON EVERY BILL:

   `WebviewWindow::print()` looks like a native silent print, but wry
   implements it as `eval("window.print()")` - it is just Chromium's print
   path, so the preview dialog always opened. Nothing in the Rust command could
   suppress it, and no earlier build ever printed silently either.

   FIX: Chromium prints straight to the default printer when started with
   `--kiosk-printing`, so the flag is passed through the window config in
   `tauri.conf.json`:

     "additionalBrowserArgs":
       "--disable-features=msWebOOUI,msPdfOOUI,msSmartScreenProtection --kiosk-printing"

   Tauri's own defaults are repeated there on purpose: supplying
   `additionalBrowserArgs` REPLACES the default string outright rather than
   adding to it, and dropping them brings back the WebView mini menu and the
   SmartScreen prompts.

   CONSEQUENCE: kiosk printing always uses the Windows DEFAULT printer. If the
   thermal printer is not the default, bills go silently to whatever is.

3. SEARCH DID NOT RANK BY CODE:

   Typing `M` matched 926 of 1,796 products and 808 of those matched only
   because a name or group contained the letter, so PUTTU MAAVU (K20) sat above
   M01. New `src/utils/productSearch.js` exports `rankProducts(products, query)`
   ordering matches:

       tier 0  code exactly equals the query
       tier 1  code starts with the query
       tier 2  code contains the query
       tier 3  English or Tamil name starts with the query
       tier 4  English or Tamil name contains the query
       tier 5  group contains the query

   Callers pass a list already sorted by code and Array.prototype.sort is
   stable, so code order survives inside each tier without a second comparator.
   An empty query returns the input untouched.

   Used by both `ProductManager.jsx` (replacing a flat `includes` filter) and
   `BillingDashboard.jsx`.

4. THE BILLING OVERLAY NEVER FILTERED, ONLY SCROLLED:

   It rendered all 1,796 products in code order and merely moved the highlight
   to the first match, so searching EGG highlighted row 129 with Fish Fry and
   Chilly Gopi still visible around it. It now lists the ranked matches, with an
   empty-state row for a query that finds nothing.

   An empty query still opens the full catalogue positioned on the first
   numeric code >= 100 - deliberately unchanged, that is how he opens the
   browse list.

   Four call sites index this list and must always move together, or the
   highlight points at the wrong row: the highlight effect, the ArrowDown
   bound, the Enter handler, and the table render.

5. A PARTIAL CODE SILENTLY BILLED THE WRONG ITEM:

   The code cell checked for an exact match, then fell through to a
   `startsWith` match and ADDED it. Typing `12` and pressing Enter put
   `120 MUTTON MASALA 100G` on the bill.

   FIX: only an exact code adds an item. Anything shorter opens the search
   overlay pre-filled with what was typed, filtered by item 4 above, so
   ArrowDown + Enter picks the right one.

   NOTE ON THE CODE DATA: 211 codes exist in both a plain and a dotted form
   (`M02` and `M02.`), and 203 codes are a strict prefix of another. Exact-match
   -first means typing `M02` always adds the plain one and never offers `M02.`;
   the dot has to be typed. That was true before this change as well.

6. FOCUS WAS LOST RETURNING FROM A PAST BILL:

   Page Up parks the current bill in `draftBill` and Page Down restores every
   field, but neither `viewNextBill` nor `restoreDraft` called `.focus()`, so
   the cursor landed nowhere and the next keystroke went into the void - the
   shopkeeper had to reach for the mouse to carry on typing.

   The draft was never at risk; only the cursor was. `activeRowIndex` and
   `activeColumn` are now stored with the draft and restored with it, focusing
   that exact cell on the same `setTimeout(..., 50)` pattern `handleClearBill`
   already used so the refs exist after the re-render. No popup, and nothing is
   written to the database.

7. FILES CHANGED:

   - `src/utils/productSearch.js`        new, shared ranking
   - `src/components/ProductManager.jsx` ranked filter
   - `src/components/BillingDashboard.jsx` filtered overlay, exact-code rule,
                                         focus restore
   - `src/components/PrintReceiptModal.jsx` print-receipt-host class
   - `src/index.css`                     print reset for the host
   - `src-tauri/tauri.conf.json`         --kiosk-printing

8. VERIFICATION:

   Driven in the browser against the live catalogue, since the shop printer was
   not to hand:

   - `M` lists M-codes first; `EGG` returns 4 items instead of 4 scattered
     through 1,796; a nonsense query shows the empty state
   - `12` + Enter opens 92 matching codes and bills nothing; ArrowDown + Enter
     adds the chosen item; `M47` + Enter adds jeera outright
   - Page Up then Page Down returns the cursor to the same cell
   - Regression each time: F10 saves exactly one bill, the counter advances, and
     the receipt still renders with the phone at the top, one total and the
     ruled lines

   CONFIRMED IN REAL USE: the shopkeeper has billed on the shop machine and a
   receipt printed successfully from the thermal printer with `--kiosk-printing`
   in place - press the key, the bill comes out, no dialog. Search, partial
   codes, focus, prices and the receipt layout are all confirmed at the counter.

   Nothing in sections 13 and 14 is left unverified.

------------------------------------------------------------------------
15. SEARCH BEHAVIOUR SPLIT & THE ENTER MONEY BUG (AUGUST 25, 2026):
------------------------------------------------------------------------
Two faults the shopkeeper found using the section 14 build at the counter. The
first REVISES items 3 and 4 of section 14 - read this before acting on those.

1. FILTERING CODE SEARCHES BROKE HOW THE SHOP NAVIGATES:

   Section 14 made the billing overlay filter every query. That was right for
   names and wrong for codes.

   Item codes are grouped by family: M1x, M2x and M3x sit next to each other and
   neighbouring codes are related goods. Typing `M2` and getting only `M2*`
   throws away the very ordering the shopkeeper browses by - he could no longer
   arrow up from M20 into the M1 range, because the M1 rows were gone.

   This is what he reported as "I can move down but I cannot move up". It was
   never a keyboard fault: ArrowUp was tested with real key presses in both
   screens and works. There was simply nothing above the match to move to.

   THE RULE NOW (`src/utils/productSearch.js`, `searchProducts`):

       if any product code STARTS WITH the query   -> JUMP
          the full catalogue stays on screen and the cursor moves to the first
          matching code, so the neighbours stay reachable either side
       otherwise                                    -> FILTER
          the ranked matches from item 3 of section 14, which still applies to
          name searches

   Which branch applies is decided from the data, not guessed from the shape of
   the text. Checked against the live catalogue:

       "M" "M2" "M4" "12" "Z1" "K" "B1"        -> jump   (M2 lands on M20)
       "EGG" "MILK" "SOAP" "RICE" "JEERA"      -> filter (EGG -> 4 items)

   A code that matches nothing - `M99` - falls through to the filter, finds no
   name either, and shows the empty state. That is intended.

   BOTH SCREENS use the same rule so one habit works everywhere. In Product
   Manager the lazily rendered window (`visibleCount`, 100 rows) is widened to
   cover the jump target, because a code can land far down the list - M.77 sits
   at index 1,059 - and the highlighted row has to exist before it can be
   scrolled to.

2. ENTER ON A FILLED ROW WIPED THE QUANTITY AND THE SLAB RATE:

   Pressing Enter on the code cell of a row that was already filled in called
   `addProductToRow` -> `executeAddProductToRow`, which reloads the product from
   the master: it hardcodes `defaultQty` and recomputes `overridePrice`. So
   stepping back to check a line and pressing Enter reset it.

   Reproduced:

       M47  0.050 kg  Rs490.00     ->  bill Rs24.50
       press Enter on the code again
       M47  1         Rs380.00     ->  bill Rs380.00

   It discarded the slab rate as well as the quantity, so a 50g jeera line
   became 1kg at the base rate. Silent, and real money.

   FIX: in `handleCellKeyDown` on the code column, if the row already holds that
   product (same code, and a name present) Enter now only advances to the
   quantity - `setActiveColumn('qty')` - and touches nothing else. Focus follows
   through the existing effect keyed on `[activeRowIndex, activeColumn]`, so no
   new focus handling was needed. A different code still loads the new product;
   an empty row still gets defaults.

3. FILES CHANGED:

   - `src/utils/productSearch.js`          `searchProducts`, `findCodeMatchIndex`
   - `src/components/BillingDashboard.jsx` overlay uses the split; Enter on an
                                           unchanged code no longer reloads
   - `src/components/ProductManager.jsx`   same split, window widened for jumps

4. VERIFICATION:

   Driven in the browser against the live catalogue:

   - `M2` jumps to M20 with M16/M18/M19 above and M21/M21./M22 below, and three
     ArrowUps walk back to M16 HORSE GRAM - the M1 range he wanted
   - `EGG` filters to 4 items, `MILK` to 14, `M99` shows the empty state
   - Product Manager behaves identically on the same queries
   - Re-entering M47 keeps 0.050 at Rs490.00 and moves focus to the quantity
   - Regression: F10 saves one bill, the counter advances, and a 50g jeera bill
     still comes to Rs24.50 gross, Rs0.50 round-off, Rs25.00 net, with the
     receipt printing phone-at-top, the round-off line and a single total

   No console errors; lint and build clean.

5. NOTE FOR NEXT TIME:

   Two bug reports in a row turned out to have the wrong cause attached - the
   "ArrowUp is broken" report was really "do not filter my code searches", and
   an earlier "printing worked before your changes" was a path that had never
   worked. Reproducing before fixing caught both. Worth doing again.

------------------------------------------------------------------------
16. SEARCH ALIGNMENT, EDITABLE CODES & REAL PRODUCT GROUPS (AUG 25-26, 2026):
------------------------------------------------------------------------
Six changes, all from counter feedback. Nothing here touches pricing, printing,
saving or bill numbering, and every one of those was re-verified afterwards.

1. A SEARCH JUMP NOW HEADS THE LIST:

   scrollIntoView used block: 'nearest' for every highlight change, which scrolls
   only far enough to reveal the row. On a code jump that left the match on the
   LAST visible line with unrelated codes above it - searching M put M.77 under
   K20-K29 - so none of its own series was in sight.

   A jump now uses block: 'start'. Arrowing afterwards deliberately stays on
   'nearest': 'start' would re-pin the cursor to the top on every keypress, so
   the rows above it could never be seen while moving up.

   TELLING THE TWO APART TOOK TWO ATTEMPTS. The scroll effect also runs on the
   render BEFORE the highlight has moved, so a plain boolean flag was consumed by
   that stale pass and only the second jump ever landed at the top. The flag now
   holds the TARGET ROW INDEX and is honoured only by the scroll that actually
   reaches it. Same pattern in both screens.

2. AND CLEARS THE STICKY HEADER:

   The header is position: sticky, top: 0, so a row scrolled to the start sat
   underneath it - the jump landed on M.77 but the first readable row was M.78.
   Rows now carry scroll-margin-top.

   THE OFFSET IS MEASURED, NOT FIXED. It is taken as the distance from the top of
   the scrolling box to the BOTTOM OF THE HEADER CELL, which is about 8px more
   than the header's own height because of the table borders - using the height
   left the row two pixels behind. It also changes with the window: 38px at
   1280px wide, 48px at 1536px. The component measures it and publishes it as
   --search-header-height; the stylesheet carries only a fallback.

   TWO MEASUREMENT TRAPS, if this is ever debugged again: `sticky` applies to the
   TH, not the THEAD, so measuring against the thead returns its unstuck document
   position and yields nonsense; and the header's rendered height is not the same
   as the clearance it needs.

3. ITEM CODES ARE EDITABLE, WITH A DUPLICATE GUARD:

   The code was locked on the edit form, so a mistyped code could only be fixed by
   deleting the product and adding it again - losing its slabs and stock. The form
   now remembers the code it opened with (`originalCode`) so a rename updates the
   right row, and that field is stripped before writing.

   On save, a code already used by ANOTHER product - compared against the original
   code, so re-saving an unchanged one is not a clash with itself - refuses the
   save, keeps the modal open and puts the cursor back on the code field.

   NOTE ON THE DATA: 211 codes exist in both a plain and a dotted form (M02 and
   M02.), and 203 codes are a strict prefix of another. Exact-match-first means
   typing M02 always adds the plain one; the dot has to be typed.

4. A RENAME CASCADES THROUGH THE BILLS:

   Bills store the item code, not a reference to the product, so renaming a code
   left every bill that used it pointing at something no longer in the catalogue:
   reprints lost the unit and price type, and deleting such a bill silently failed
   to put the stock back.

   A rename now rewrites the matching line items in database.transactions and
   saves them with the products. Matching is case-insensitive on the old code, the
   new code is written as entered, and the transaction array is only replaced when
   a rename actually happened - an ordinary price edit leaves it byte-identical.
   The count of updated lines goes to app.log.

   Verified on the real 1,850-bill history: renaming M47 moved all 197 of its line
   items, with the bill count and the Rs11,58,415 total unchanged and every line
   keeping its own name, quantity and rate.

5. RESTORING AN ARCHIVE REMAPS OLD CODES:

   The cascade above covers the live ledger. An archive written BEFORE a rename
   still holds the old code, and nothing else would say what it became.

   `archive_history.mjs --restore` now resolves each archived code against two
   sources, strongest first:

     settings.codeRenames   an exact record written by Product Manager at the
                            moment of each rename. Chains are followed, so
                            A -> B -> C resolves to C.
     the product name       for renames made before that log existed. Used ONLY
                            when exactly one product carries the name - six names
                            in this catalogue are shared by two products.

   Anything neither can place keeps its original code and is REPORTED with a
   count, never guessed at. Tested on a copy of the real archive with two renames
   staged, one recorded and one not: 197 line items resolved through the log, 64
   through the name, none unresolved.

6. THE SHOP'S REAL PRODUCT GROUPS ARE BACK:

   The August import lost the grouping - products arrived as "General" or as a GST
   bucket like "05% 15121910" - so the group filter was useless. The real groups
   were still in the FoxPro data: IM012026.DBF carries each item's IGROUPCODE and
   IG012026.DBF maps that to a name.

       node tari/scripts/restore_product_groups.mjs --dry-run
       node tari/scripts/restore_product_groups.mjs
       node tari/scripts/restore_product_groups.mjs --db=tari/src-tauri/seed_database.json

   All 1,803 products resolve to one of 35 groups with nothing left over, and the
   counts match the shop's own figures: 121 M.MALIGAI, 120 G1.CASH,MUTTAI, 93
   W.WASINGSOAP/POWDER, 89 4.POOJA PRODUCTS, down to 1. Both installs under
   Old_sms hold these tables and agree item for item.

   ONLY the `group` field is written - the script aborts if any other field
   differs on any product. Names are kept verbatim, including the double space in
   "01DALL ITEMS   RST".

   Applied to the live database AND the bundled seed, so a fresh install starts
   grouped too.

   Both dropdowns list the groups alphabetically. "General" is deliberately NO
   LONGER OFFERED: it was the import's placeholder, and keeping it selectable
   would let products drift back out of the shop's scheme. A new product starts in
   Z.OTHERS, the shop's own catch-all.

   ONE BEHAVIOUR CHANGES AS A CONSEQUENCE, and it is an improvement: searching a
   word that is also part of a group name now surfaces that group BELOW the exact
   name matches. EGG returns the four egg products first, then the 25 items in
   5.MILKPROD/EGG. Group matching has always been the lowest search tier (see
   productSearch.js); it simply never matched anything while every group was
   called "General". If the extra rows are ever unwanted, dropping that tier is a
   one-line change.

7. ZERO-REGRESSION PASS AFTER ALL OF THE ABOVE:

   - code queries jump, name queries filter, on both screens
   - ArrowUp out of a jump walks back into the neighbouring code range
   - header clearance holds at 7-8px on both screens
   - Enter on a filled row keeps 0.050 at Rs490.00
   - duplicate code refused; rename cascades and is recorded
   - slabs: 50g Rs24.50, 100g Rs49.00, 250g Rs95.00, 500g Rs190, 1kg Rs380
   - F10 saves one bill, Rs24.50 gross and Rs25.00 net, counter advances
   - receipt prints the phone at the top, a single total and the round-off
   - no console errors; lint and build clean

8. A MISTAKE MADE AND REPAIRED, RECORDED SO IT IS NOT REPEATED:

   While syntax-checking archive_history.mjs, `node -e "import(...)"` was used.
   That RUNS the module rather than parsing it, and the script executed in ARCHIVE
   mode against the live database, moving one bill (#1, 24/08/2026, Rs380) into a
   stray archive file. It was merged straight back, the stray file deleted and the
   bill counter left untouched.

   Use `node --check <file>` to parse without executing.

------------------------------------------------------------------------
9. PREVIOUS BILL FOCUS & TOP ROW SCROLL CLEARANCE FIX:
------------------------------------------------------------------------
When viewing or clicking "Edit" on a saved bill (e.g. 15-item bill), focus previously
jumped to the newly appended empty row at the bottom (Row 16 / index 15), scrolling
the table down and hiding Rows 1, 2, 3 off-screen.

Fix details:
- In `BillingDashboard.jsx`:
  - `loadTransactionToView`: Explicitly sets `setActiveRowIndex(0)`, `setActiveColumn('code')`,
    and resets `billingTableContainerRef.current.scrollTop = 0`.
  - `handleStartEditSavedBill`: Changed focus target from `firstEmptyIndex` to `0` (Row 1, code column),
    resetting `scrollTop = 0` so top items remain immediately visible.
  - Table traversal: Added `activeBillingRowRef` with dynamic `--search-header-height`
    measurement and added `.billing-table tbody tr` to `src/index.css` under
    `scroll-margin-top: calc(var(--search-header-height, 48px) + 6px);`.
  - Arrowing up back to Row 1 guarantees 0px clipping below the sticky green header.

------------------------------------------------------------------------
11. SEPARATE TAMIL AND ENGLISH PRODUCT COLUMNS IN BILLING GRID:
------------------------------------------------------------------------
The search overlay menu and Product Manager have always placed Tamil Name first
in its own column and English Name second in its own column. The main billing table
previously combined them into a single column with English on top and small Tamil below.

Fix details:
- In `BillingDashboard.jsx` (Main Billing Grid for both New Bills and Saved Bills):
  - Split the combined column into two distinct columns:
    Col 3: `தமிழ் பெயர் / Tamil Name` (Bold font, Tamil-first)
    Col 4: `பொருள் / Product Name (Eng)`
  - Rebalanced column widths: S.No (50px), Code (100px), Tamil Name (flex), English Name (flex),
    Unit (80px), Qty (100px), M.R.P (100px), Rate (110px), Total (130px), Delete (40px).
- In `SalesHistory.jsx`:
  - Aligned invoice inspection modal table to also separate Tamil and English columns.
- Unified 1:1 column consistency across:
  1. Entering New Bill
  2. Viewing Saved Bill (`PageUp` / `PageDown`)
  3. Product Search Menu (`Enter` on empty code)
  4. Product Manager Master Shelf
  5. Sales History Inspection Modal

------------------------------------------------------------------------
12. THERMAL RECEIPT TOTAL AMOUNT ENLARGEMENT (BIG & BOLD):
------------------------------------------------------------------------
On printed 80mm thermal receipts, the blanket 12px print rule previously forced
the final total line (`மொத்தம் / Total : ₹795.00`) down to body text size, making
it small and unnoticeable.

Fix details:
- In `src/index.css` (@media print):
  - Added `.print-receipt-area .receipt-total-row, .print-receipt-area .receipt-total-row *`:
    `font-size: 18px !important; font-weight: 900 !important; -webkit-text-stroke: 0.5px #000000 !important;`
  - Added `.print-receipt-area .receipt-total-amount`:
    `font-size: 20px !important; font-weight: 900 !important; letter-spacing: 0.02em !important; -webkit-text-stroke: 0.6px #000000 !important;`
- In `PrintReceiptModal.jsx`:
  - Applied `.receipt-total-row`, `.receipt-total-label`, and `.receipt-total-amount`.
  - Adjusted calculation block padding from `20px` to `4px` so the total row spans the full
    width of the 72mm thermal paper.
- Verified in automated print media browser test (`test_receipt_print_total.cjs`):
  - Label rendered at 18px / 900 weight / 0.5px stroke.
  - Amount rendered at 20px / 900 weight / 0.6px stroke (extra bold, clear burn).

========================================================================

